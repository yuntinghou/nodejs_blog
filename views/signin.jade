include header
.ui.grid
    .four.wide.column
    .eight.wide.column
        form(class="ui form segment" method="post")
            .field.required
                label Username
                input(placeholder="Username" type="text" name="name")
            .field.required
                label Password
                input(placeholder="Password" type="password" name="password")
            input(type="submit" class="ui button fluid" value="Sign In")
//-var user = {_id:'12', name: 'yunting', gender:"m", bio: "oye" }
//-var gender = {m : 'Male', f : 'Female', x : 'Unknown'}
//.ui.grid
//    .four.wide.column
//    .eight.wide.column
//        a(class="avatar" href="/posts?author=#{user._id}" data-title="#{user.name} | " + gender[user.gender] data-content="#{user.bio}") Oyeddd
include footer